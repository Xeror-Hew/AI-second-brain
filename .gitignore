# Meta-workspace only (not part of the published blueprint).
# Anchored with a leading slash so the blueprint's own .claude/ and CLAUDE.md still ship.
/.claude/
/CLAUDE.md
/test/
/historico/
/history/

# OS junk
blueprint_feedback.md
writing-style-guide.md
.DS_Store
Thumbs.db

# Obsidian app config: each user gets their own when they open the vault
.obsidian/
